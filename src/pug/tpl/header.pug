header.header
  .wrapper.p-half-v
    .grid.grid-negative
      .grid__item.one-half
        .logo
          img.logo-img(
            alt="Showapp"
            src="assets/img/logo.png"
          )
          h1.logo-title
            = title
      .grid__item.one-half
        nav.menu
          a.menu__item.menu__item-active(href="#")
            | Movies
          a.menu__item(href="#")
            | Series
          a.menu__item(href="#")
            | Favoritos
  include partials/filter.pug
include partials/list.pug
