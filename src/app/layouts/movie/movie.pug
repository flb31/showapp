section.shows.p-t
  .wrapper
    .grid.grid-negative.overflow-visible.clear
      .grid__item.one-whole.mid--one-half.m-b(ng-repeat="item in movieCtrl.getShows()")
        show-app-show-card(item="item")
